export type LeavesReport = {
  availed: number;
  available: number;
  pending: number;
};
export type RequestModalType = {
  Subject: string;
  Description: string;
  EndAppDate: string;
  StartAppDate: string;
  LeaveTypeId: string;
};
